services:

  # ── PHP / Symfony ────────────────────────────────────────────────
  php:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: orbitview_php
    restart: unless-stopped
    volumes:
      - ./backend:/var/www/html
    environment:
      APP_ENV: dev
      DATABASE_URL: "postgresql://orbitview:secret@db:5432/orbitview?serverVersion=16&charset=utf8"
      REDIS_URL: "redis://redis:6379"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    healthcheck:
      test: ["CMD", "php", "bin/console", "about", "--env=prod", "--no-debug"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 20s
    networks:
      - orbitview

  # ── Scheduler Symfony (TLE toutes les 2h) ────────────────────────
  scheduler:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: orbitview_scheduler
    restart: unless-stopped
    command: ["/bin/sh", "/var/www/html/scheduler-entrypoint.sh"]
    volumes:
      - ./backend:/var/www/html
    environment:
      APP_ENV: prod
      DATABASE_URL: "postgresql://orbitview:secret@db:5432/orbitview?serverVersion=16&charset=utf8"
      REDIS_URL: "redis://redis:6379"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
      php:
        condition: service_healthy
    networks:
      - orbitview

  # ── Nginx ────────────────────────────────────────────────────────
  nginx:
    image: nginx:alpine
    container_name: orbitview_nginx
    restart: unless-stopped
    ports:
      - "8080:80"      # API  → http://localhost:8080
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./backend/public:/var/www/html/public:ro
    depends_on:
      - php
    networks:
      - orbitview

  # ── PostgreSQL ────────────────────────────────────────────────────
  db:
    image: postgres:16-alpine
    container_name: orbitview_db
    restart: unless-stopped
    ports:
      - "5432:5432" 
    environment:
      POSTGRES_DB: orbitview
      POSTGRES_USER: orbitview
      POSTGRES_PASSWORD: secret
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U orbitview"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - orbitview

  # ── Redis ────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: orbitview_redis
    restart: unless-stopped
    command: redis-server --maxmemory 128mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    networks:
      - orbitview

volumes:
  pg_data:
  redis_data:

networks:
  orbitview:
    driver: bridge
